openapi: "3.0.0"
info:
  title: XSS Test
  version: "1.0.0"
paths:
  /test:
    get:
      summary: test
      description: |
        [click me](javascript:alert\(document.domain\))
      responses:
        "200":
          description: ok
